# Employia Landing Page

Modern React/Vite landing page for Employia with cinematic scrolling, animated network visuals, magnetic CTA buttons, and responsive sections for roles, founders, talent, and contact.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:5173/`.

## Build

```bash
npm run build
```
